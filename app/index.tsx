import { View, Text } from "react-native"
import { SafeAreaView } from "react-native-safe-area-context";

const Home = () => {
    return (
        <SafeAreaView>
            <Text className="font-medium text-3xl">Home Page</Text>
        </SafeAreaView>
    )
}

export default Home;